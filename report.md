# 과제 02 – 이진트리 프로그램 구현 (설명 문서)

언어: C (C99) / 입력: 괄호 표기법 / 구현 2종: 배열, 포인터(연결 자료구조)

## 1. 파일 구성과 실행 방법

| 파일 | 역할 |
|---|---|
| `tree_common.h` | 공통 도구: 괄호 표기법 렉서, 메모리 사용량 추적, 결과 출력 함수 |
| `tree_array.h` | **1. 배열 구현** (입력, 출력, 트리 정보, 형태 판별, 관계 조회, 메모리) |
| `tree_linked.h` | **2. 연결 구현** (같은 기능을 포인터로 구현) |
| `main_array.c` | 배열 구현 메뉴 프로그램 |
| `main_linked.c` | 연결 구현 메뉴 프로그램 |
| `compare.c` | **3. 비교 프로그램** (메모리 실측, 두 구현 결과 검증, 관계 조회 비용 측정) |
| `Makefile` | `make`로 세 프로그램을 한 번에 빌드 |

```
make                                  # tree_array, tree_linked, tree_compare 생성
./tree_array                          # 트리를 직접 입력하는 메뉴 프로그램
./tree_array "A(B(D,E),C)" --all      # 인자로 준 트리에 대해 모든 기능을 한 번에 실행
./tree_linked "A(B(D,E),C)"           # 연결 구현 (메뉴)
./tree_compare                        # 3번 비교 실험 실행
```

Makefile 없이 직접 빌드: `gcc -std=c99 -Wall -Wextra -o tree_array main_array.c` (Windows: `-o tree_array.exe`,
Visual Studio 명령줄은 `cl /utf-8 main_array.c`). 콘솔 한글 출력을 위해 Windows에서는 프로그램이 시작할 때
출력 코드 페이지를 UTF-8로 바꾼다.

메뉴: `[1]` 이진트리 출력, `[2]` 트리 정보, `[3]` 형태 판별, `[4]` 자식/부모/형제 조회(3-[2]), `[5]` 메모리 사용량, `[6]` 새 트리 입력.

## 2. 입력 형식 (괄호 표기법)

```
tree := 이름 [ '(' [tree] [ ',' [tree] ] ')' ]
```

| 입력 | 의미 |
|---|---|
| `A(B(D,E),C)` | A의 왼쪽 자식 B(자식 D, E), 오른쪽 자식 C |
| `A(,C)` | 왼쪽 자식이 없고 오른쪽 자식만 C (**왼쪽이 비면 괄호 앞 자리를 비워 둔다**) |
| `A(B)` 또는 `A(B,)` | 왼쪽 자식 B만 있음 |
| `A` | 단말 노드 하나 |

노드 이름은 공백/괄호/쉼표가 아닌 문자로 최대 7글자입니다. 자식이 3개 이상이거나, 괄호가 맞지 않거나,
끝에 불필요한 문자가 있으면 몇 번째 문자에서 문제인지 알려 주고 다시 입력받습니다.
이름이 같은 노드가 있으면 경고를 출력합니다(관계 조회는 처음 찾은 노드 기준).

## 3. 두 가지 표현 방식

### 3.1 배열 (`tree_array.h`)

트리가 "꽉 찬(포화) 이진트리"라고 가정하고 노드에 레벨 순서로 번호를 붙여 그 번호를 배열 인덱스로 씁니다.
0번 칸은 쓰지 않고 루트가 1번입니다.

```
루트      = slot[1]
노드 i 의 왼쪽 자식  = slot[2i]
노드 i 의 오른쪽 자식 = slot[2i+1]
노드 i 의 부모      = slot[i/2]
노드 i 의 형제      = slot[i ^ 1]     (짝수면 i+1, 홀수면 i-1)
```

빈 자리는 `name[0]=='\0'`인 빈 슬롯입니다. `A(B(D,E),C(,F))`는 다음과 같이 저장됩니다.

```
[1]=A [2]=B [3]=C [4]=D [5]=E [6]=- [7]=F
```

배열 크기는 "가장 큰 인덱스 + 1"에 딱 맞게 늘립니다(`realloc`). 편향 트리는 필요한 크기가 2^n으로 커지므로
안전장치로 슬롯 수 4,194,304개(32 MB)를 넘으면 입력을 거절하고 이유를 알려 줍니다.

### 3.2 연결 자료구조 (`tree_linked.h`)

```c
typedef struct Node {
    char name[8];
    struct Node *left, *right;     // 자식이 없으면 NULL
} Node;
```

노드를 하나 읽을 때마다 `malloc`으로 한 개씩 만들고 포인터로 연결합니다. 부모 포인터는 두지 않았습니다.

## 4. 기능별 구현 방법

### 4.1 이진트리 출력 (왼쪽으로 눕힌 형태)

트리를 반시계 방향으로 90도 눕히면 루트가 왼쪽, 오른쪽 자식이 위, 왼쪽 자식이 아래가 됩니다.
"오른쪽 서브트리 → 자신 → 왼쪽 서브트리" 순서로 재귀 호출하고, 깊이 × 4칸만큼 들여써서 출력하면 됩니다(두 구현 모두 같은 방식).

```
$ ./tree_array "A(B(D,E),C)" --all
    C
A
        E
    B
        D
```

### 4.2 트리 정보

| 항목 | 배열 구현 | 연결 구현 |
|---|---|---|
| 전체 노드 수 | 비어 있지 않은 슬롯 수 | `1 + count(left) + count(right)` |
| 단말 노드 수 | 자식 슬롯 2i, 2i+1이 모두 빈 노드 | left, right가 모두 NULL |
| 비단말 노드 수 | 전체 − 단말 | 전체 − 단말 |
| 높이 | 가장 큰 사용 인덱스의 레벨 = `floor(log2 i)+1` | `1 + max(높이(left), 높이(right))` |
| 차수 | 각 노드의 자식 수(0~2) 중 최댓값 | 각 노드의 자식 수 중 최댓값 |

높이는 **루트의 레벨을 1로 보는 정의**(노드 1개짜리 트리의 높이 = 1)를 기본으로 출력하고, 간선 수 기준 값도 괄호로 함께 보여 줍니다.
차수는 이진트리이므로 0, 1, 2 중 하나입니다(트리의 차수 = 노드 차수의 최댓값).

### 4.3 형태 판별

| 형태 | 정의 | 배열 구현 | 연결 구현 |
|---|---|---|---|
| 완전 이진트리 | 레벨 순서로 1..n번 자리가 빈틈없이 채워진 트리 | 노드가 n개일 때 `slot[1..n]`이 모두 차 있는지 검사 | 레벨 순서(BFS)로 방문하다가 NULL 자식을 본 뒤에 진짜 노드가 또 나오면 "아니오" |
| 포화 이진트리 | 높이 h인데 노드 수가 정확히 2^h − 1 | 높이와 노드 수 비교 | 모든 노드에서 좌·우 서브트리 높이가 같은지 재귀 검사 |
| 편향 이진트리 | 모든 노드가 자식을 1개 이하만 가지고 방향이 모두 같음 | 슬롯을 훑으며 자식 방향 기록 | 재귀로 자식 방향 기록 |

정의에서 판단이 갈릴 수 있는 경우는 다음과 같이 정했습니다.

* 포화 이진트리는 항상 완전 이진트리이기도 합니다(예: 노드 15개 완전 트리는 포화이기도 함).
* 노드가 1개뿐인 트리는 완전, 포화, 편향(자명한 경우)에 모두 "예"로 처리하고 이유를 함께 출력합니다.
* 편향은 **모든 노드가 왼쪽 자식만**(왼쪽 편향) 또는 **모두 오른쪽 자식만**(오른쪽 편향) 가질 때만 "예"입니다.
  자식은 1개씩이지만 방향이 섞인 지그재그형(`A(B(,C))`)은 "아니오"로 하고, 이유를 함께 출력합니다.

## 5. 비교 실험 결과 (`./tree_compare` 실행 결과)

`sizeof(Slot)=8 B`, `sizeof(Node)=24 B (이름 8 + 포인터 2×8)`, 트리 구조체는 배열 16 B, 연결 8 B입니다.
아래 크기는 `malloc`/`realloc`으로 **요청한 바이트 수**를 프로그램 안에서 세어서 얻은 값이며,
계산식(슬롯 수 × 8 또는 노드 수 × 24)과 일치하는 것도 프로그램이 확인합니다.
실제 할당기의 블록 머리말/정렬 오버헤드는 포함하지 않았습니다.

### 5.1 [3-1] 메모리 사용량 비교

**(a) 노드 수를 비슷하게 맞춘 세 종류의 트리** (`array(B)`, `linked(B)`는 구조체 포함 총 바이트)

```
    n height     slots    array(B)   linked(B)  arr/link      fill  type
   15      5        28         240         368      0.65     53.6%  일반 이진트리
   15      4        16         144         368      0.39     93.8%  완전 이진트리 (포화)
   12      4        13         120         296      0.41     92.3%  완전 이진트리 (비포화)
   15     15     16385      131096         368    356.24      0.1%  왼쪽 편향 이진트리
   15     15     32768      262160         368    712.39      0.0%  오른쪽 편향 이진트리
```

**(b) 완전 이진트리: 노드가 늘어날 때**

```
    n height     slots    array(B)   linked(B)  arr/link      fill  type
    7      3         8          80         176      0.45     87.5%  완전 이진트리
   15      4        16         144         368      0.39     93.8%  완전 이진트리
   63      6        64         528        1520      0.35     98.4%  완전 이진트리
  255      8       256        2064        6128      0.34     99.6%  완전 이진트리
 1023     10      1024        8208       24560      0.33     99.9%  완전 이진트리
```

**(c) 왼쪽 편향 이진트리: 노드가 늘어날 때**

```
    n height     slots    array(B)   linked(B)  arr/link      fill  type
    5      5        17         152         128      1.19     29.4%  왼쪽 편향
   10     10       513        4120         248     16.61      1.9%  왼쪽 편향
   15     15     16385      131096         368    356.24      0.1%  왼쪽 편향
   20     20    524289     4194328         488   8594.93      0.0%  왼쪽 편향
```

노드 23개짜리 편향 트리는 배열로는 슬롯이 2^22개(32 MB)를 넘어서 프로그램이 입력을 거절합니다.
연결 구현은 23 × 24 = 552 B면 충분합니다.

#### 실측 결과로 본 설명

1. **완전 이진트리에서는 배열이 가장 작습니다.** 슬롯 수가 n+1(0번 칸 포함)이라 배열은 약 8n B이고,
   연결 구현은 노드마다 포인터 2개가 더 붙어서 약 24n B입니다. 그래서 n이 커질수록 `arr/link`가 1/3(0.33)에 수렴합니다
   (n=1023에서 8,208 B 대 24,560 B). 배열은 자식 위치를 인덱스로 계산하므로 포인터를 저장할 필요가 없기 때문입니다.
2. **일반 이진트리에서는 빈 슬롯이 낭비입니다.** 노드 15개인데 가장 큰 인덱스가 27이라 슬롯 28개를 잡고
   활용률이 53.6%입니다. 그래도 슬롯 하나(8 B)가 노드 하나(24 B)의 1/3이라 배열이 여전히 더 작습니다(240 B 대 368 B).
3. **편향 이진트리에서는 배열이 폭발합니다.** 깊이가 h이면 마지막 노드의 인덱스가 2^(h−1)이 되므로 슬롯 수가 2^(n−1)+1로 늘어납니다.
   노드가 15개뿐인데 왼쪽 편향은 131,096 B로 연결 구현(368 B)의 약 356배, 오른쪽 편향은 인덱스가 2^n − 1까지 가서 약 712배입니다.
   활용률은 0.1%도 안 됩니다.
4. **손익분기점.** 총 바이트는 대략 배열 8·(슬롯 수), 연결 24·n 이므로
   `8·슬롯 수 < 24·n`, 즉 **활용률 n/슬롯 수 > 1/3 이면 배열이 유리**하고 그보다 낮으면 연결이 유리합니다.
   실측과 정확히 맞습니다: 일반(53.6%)과 완전(87% 이상)은 배열 승, 편향 n=5(29.4%)는 연결 승(152 B 대 128 B).
5. **(참고) 연결 구현은 노드마다 `malloc`을 한 번 호출**해서 블록이 n개 생기고, 실제 시스템에서는
   블록마다 관리용 머리말과 정렬 낭비가 더 붙습니다. 배열 구현은 블록이 1개입니다. 표의 값은 이 오버헤드를 넣지 않은 값이라
   실제 차이는 표보다 연결 쪽이 조금 더 불리합니다. 다만 편향 트리에서 배열이 수백~수천 배 큰 것을 뒤집을 정도는 아닙니다.

### 5.2 [3-2] 자식/부모/형제 조회와 효율 비교

두 프로그램의 메뉴 `[4]`에서 노드 이름을 입력하면 부모, 왼쪽/오른쪽 자식, 형제를 출력하고 **접근한 슬롯/노드 수**를 함께 보여 줍니다.

| | 배열 구현 | 연결 구현 |
|---|---|---|
| 노드 찾기(이름 → 위치) | 슬롯을 1번부터 훑음 | 루트부터 전위 순회 |
| 자식 | `slot[2i]`, `slot[2i+1]` | `p->left`, `p->right` |
| 부모 | `slot[i/2]` | 부모 포인터가 없어 **루트부터 다시 탐색** |
| 형제 | `slot[i^1]` | 부모를 찾은 뒤 부모의 다른 쪽 자식 |

**측정 결과** (find: 이름으로 노드를 찾는 데 접근한 횟수, rel: 그 노드의 부모·자식·형제를 얻는 데 접근한 횟수)

```
  tree             node  | array: find rel total | linked: find rel total |
  general(15)      1     |      1      2      3 |      1      2      3 | 결과 동일
  general(15)      3     |      3      4      7 |      9      4     13 | 결과 동일
  general(15)      15    |     19      4     23 |      6      8     14 | 결과 동일
  complete(15)     15    |     15      4     19 |     15     16     31 | 결과 동일
  left-skew(15)    8     |    128      4    132 |      8     10     18 | 결과 동일
  left-skew(15)    15    |  16384      4  16388 |     15     17     32 | 결과 동일
  right-skew(15)   15    |  32767      4  32771 |     15     17     32 | 결과 동일
  complete(1023)   1023  |   1023      4   1027 |   1023   1024   2047 | 결과 동일
  complete(1023)   511   |    511      4    515 |   1021   1020   2041 | 결과 동일
  left-skew(20)    20    | 524288      4 524292 |     20     22     42 | 결과 동일
```

#### 분석

1. **관계 계산(rel)은 배열이 압도적으로 효율적입니다.** 배열은 트리 크기와 상관없이 항상 4번 이하(루트는 2번)의 접근으로
   부모/자식/형제를 얻습니다(인덱스 계산 `i/2`, `2i`, `2i+1`, `i^1` → O(1)). 연결 구현은 자식은 O(1)이지만
   **부모와 형제는 부모 포인터가 없어서 루트부터 다시 찾아야 하므로 O(n)** 입니다. 완전 트리 1023개에서 rel이 4번 대 1,020~1,024번입니다.
2. **이름으로 노드를 찾는 비용(find)은 둘 다 O(n) 계열입니다.** 다만 배열은 빈 슬롯까지 훑기 때문에
   완전 트리에서는 연결과 비슷하지만(1023 대 1023), 편향 트리에서는 슬롯이 폭발해서 찾는 비용도 폭발합니다
   (left-skew(20)에서 524,288번 대 20번). 즉 편향/희소 트리에서는 총비용이 연결 구현이 훨씬 작습니다.
3. **위치를 이미 알고 있다면(인덱스 또는 포인터를 가진 경우) 차이가 더 분명합니다.** 배열은 find 없이 곧바로 O(1)로 모든 관계를 얻습니다.
   연결 구현도 자식은 O(1)이지만 부모/형제는 O(n)입니다. 연결 노드에 `parent` 포인터를 추가하면 O(1)이 되지만
   노드가 24 B에서 32 B로 커지고(약 33% 증가) 삽입/삭제 때 부모 포인터도 관리해야 합니다.
4. **결론.** 부모/자식/형제 접근만 보면 **배열 구현이 더 효율적**이며 완전 이진트리(힙 등)에서는 메모리도 가장 작아서 이상적입니다.
   반대로 트리가 한쪽으로 치우치거나 듬성듬성하면 배열은 메모리와 탐색 모두 낭비가 커지므로
   **일반/편향 이진트리나 노드 삽입·삭제가 잦은 경우에는 연결 구현이 알맞습니다.**

| 관점 | 배열 | 연결 |
|---|---|---|
| 완전 이진트리 메모리 | 약 8n B (유리) | 약 24n B |
| 편향/희소 트리 메모리 | 2^n 으로 폭발 | 24n B (유리) |
| 부모/형제 접근 | O(1) | O(n) (부모 포인터가 없을 때) |
| 자식 접근 | O(1) | O(1) |
| 이름으로 탐색 | O(슬롯 수) | O(n) |
| 삽입/삭제(중간 노드) | 서브트리 통째로 이동이 필요할 수 있음 | 포인터만 바꾸면 됨 |

## 6. 검증

* `compare.c`가 같은 입력(일반, 완전, 포화, 편향, 단일 노드, 지그재그 등 8종)에 대해 두 구현의 노드 수, 단말 수, 높이, 차수, 완전/포화/편향 판별 결과가
  모두 같은지 자동으로 비교하며, 실행 결과 "모든 경우 일치"입니다. 관계 조회 결과도 모든 측정 케이스에서 동일했습니다.
* `-Wall -Wextra -pedantic` 옵션으로 경고 없이 빌드되고, AddressSanitizer/UBSan으로 실행해서 메모리 오류나 누수가 없음을 확인했습니다.
* 잘못된 입력(`A(B,C,D)`, `A(B(C)`, `A(B,C))`, 이름이 8글자 이상, 빈 입력 등)은 두 구현 모두 같은 위치에서 같은 오류 메시지를 냅니다.

## 7. 실행 예시

```
$ ./tree_linked "A(B(D,E),C(,F))" --all
        F
    C
A
        E
    B
        D

  1. 전체 노드의 수       : 6
  2. 단말 노드의 수       : 3
  3. 비단말 노드의 수     : 3
  4. 트리의 높이(height)  : 3  (루트 레벨=1 기준, 간선 수 기준이면 2)
  5. 트리의 차수(degree)  : 2

  완전 이진트리 : 아니오
  포화 이진트리 : 아니오
  편향 이진트리 : 아니오
```
